#include <cstdio>
#include <iostream>
#include <cmath>

int main()
{
    freopen("INPUT.INP", "r", stdin);
    freopen("OUTPUT.OUT", "w", stdout);
    int x = 0;
    double tong = 0;
    std::cin >> x;
    tong = cbrt(x);

    return 0;
}